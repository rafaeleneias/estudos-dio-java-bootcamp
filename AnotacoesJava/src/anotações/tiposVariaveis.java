package anotações;
public class tiposVariaveis {
    public static void main(String[] args) throws Exception {
      // Variável é uma identificação de um espaço em memória utilizado pelo nosso programa. Na linguagem Java, toda variável é
       // composta por tipo de dado + identificação + valor atribuído. Ex:
       int idade = 20;
       int ano = 2023;
       String instituição = "DIO";
       double salario = 2500.00;
       System.out.println(idade);
       System.out.println(ano);
       System.out.println(instituição);
       System.out.println(salario);
     // Constantes são tipos de dados que não podem ser alteradas ao longo do programa. Na linguagem Java, esses valores são
     // representadas pela palavra reservada final. Porém, por convenção, a palavr deverá ser escrita em caixa alta.Ex:
        final int ANOATUAL = 2023; // não poderá ser mudado ao longo do programa.
    // Char: Apenas um caractere. Ex: 'M'.
    // Boolean: Verdadeiro ou falso.
    // Void indica que o método não retorna nenhum valor

    }
}
